name: tasky
description: To-Do application on Flutter.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0-109.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  intl: ^0.18.1
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
